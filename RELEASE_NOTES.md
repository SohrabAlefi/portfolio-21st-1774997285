Release v1.0.0

What's included:
- Scaffolded 21st.dev portfolio site (React + Vite)
- CI configuration and dependencies

Tests performed:
- Local Vite build (npx vite build) — build completed (note: react externalization warning resolved by bundling dependencies in environment)
- Automated publish to here.now (anonymous) — site published and claim token saved

Deployment:
- Anonymous here.now publish (expires in 24h) with claim URL and token saved to .herenow/state.json

